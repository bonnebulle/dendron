Root project :
/home/pi/Dendron/
_ = allias


# Dendron Build (yarn)
_ buildd
yarn dendron-cli buildSiteV2 --wsRoot .  --stage prod"




# Dendron lanch engine
_ denserv
dendron dendron launchEngineServer --wsRoot ~/Dendron/ --port `cat .dendron.port`



# Dendron alt preview ( engine NOT lanched )
_ denprev
echo need denserv && echo `cat .dendron.port` && echo exec from /home/pi/Drendon/ folder ! && 

sudo npx dendron buildSiteV2 --wsRoot . --stage dev --serve --enginePort `cat .dendron.port`

"





*from ~/Dendron/ 
("cat .dendron.port" récupère le port random de dendron)